#include <iostream>
using namespace std;

string checkNumber(int n) {
    if (n > 0)
        return "Positive";
    else if (n < 0)
        return "Negative";
    else
        return "Zero";
}

int main() {
    int num;
    cin >> num;
    cout << checkNumber(num) << endl;

    return 0;
}