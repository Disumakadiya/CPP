#include <iostream>
#include <cmath>   
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int N;
        cin >> N;
        int result = sqrt(N); 
        cout << result << endl;
    }
    return 0;
}
